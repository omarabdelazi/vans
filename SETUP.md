# إعداد ونشر متجر VANS على Cloudflare

الموقع بيتنشر على **Cloudflare Pages** والـ API شغال بـ **Pages Functions** مع قاعدة بيانات
**D1** وتخزين صور **R2**. الجداول بتتعمل تلقائيًا أول ما الموقع يشتغل — مفيش أي أوامر SQL مطلوبة منك.

## خطوات الإعداد (مرة واحدة بس)

### 1) إنشاء قاعدة البيانات D1
1. من لوحة Cloudflare: **Storage & Databases → D1 → Create database**
2. سمّيها مثلاً: `vans-db`

### 2) إنشاء مخزن الصور R2
1. من اللوحة: **R2 Object Storage → Create bucket**
2. سمّيه مثلاً: `vans-files`
3. لو أول مرة تستخدم R2 هيطلب تفعيل الخدمة (فيه خطة مجانية).

> صور المنتجات وإيصالات التحويل (انستا باي / فودافون كاش) بتتخزن هنا.

### 3) ربط القاعدة والمخزن بمشروع Pages
من مشروع **vans** في Pages: **Settings → Bindings** (أو Functions):

| النوع | الاسم (لازم بالظبط كده) | القيمة |
|---|---|---|
| D1 database | `DB` | `vans-db` |
| R2 bucket | `FILES` | `vans-files` |

### 4) متغيرات البيئة (كلمة سر الداشبورد)
من **Settings → Environment variables** ضيف للـ Production:

| الاسم | القيمة |
|---|---|
| `ADMIN_PASSWORD` | كلمة السر اللي هتدخل بيها على الداشبورد |
| `SESSION_SECRET` | أي جملة طويلة عشوائية (32+ حرف) — لتوقيع الجلسات |

### 5) إعادة النشر
بعد إضافة الـ Bindings والمتغيرات، اعمل **Retry deployment** (أو ادفع أي commit جديد)
علشان الإعدادات الجديدة تتفعل.

## بعد النشر

- **الداشبورد**: `https://your-site.pages.dev/admin` — ادخل بكلمة السر.
- من **الإعدادات** جوّه الداشبورد: سجّل رقم انستا باي ورقم فودافون كاش (بيظهروا للعميل
  في صفحة الدفع) وتليفون وعنوان المحل (بيظهروا في الريسيت).
- من **الأقسام** و **المنتجات**: ضيف الأقسام والمنتجات بالمقاسات (35 → 44) والكميات.
- خانة "رابط موديل 3D" في المنتج: حط `/models/shoe.glb` للتجربة، أو ارفع ملف GLB
  لموديل منتجك وحط رابطه — وفعّل AR علشان العميل يجرب الشوز بكاميرا الموبايل.

## دورة الشغل اليومية

- **طلبات الأونلاين** بتوصل "قيد المراجعة" — راجع صورة التحويل واضغط **تأكيد**
  (ساعتها بس بيتخصم من المخزون). الرفض بيرجّع الكمية تلقائيًا.
- **البيع في المحل** من صفحة **نقطة البيع**: دور بالكود → اختار المقاس → إتمام البيع —
  بيخصم من المخزون فورًا ويطبعلك **ريسيت** للعميل.
- **رسائل العملاء** من صفحة "اتصل بينا" بتوصلك في **الرسائل**.

## تشغيل محلي (للتطوير)

```sh
npm install
npm run build
npx wrangler pages dev dist --d1 DB --r2 FILES --port 8788
```

وحط كلمة السر في ملف `.dev.vars`:

```
ADMIN_PASSWORD=test1234
SESSION_SECRET=dev-secret
```
